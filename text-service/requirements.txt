# ── Web framework ─────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── ONNX Runtime thuần CPU — KHÔNG kéo torch ─────────────
onnxruntime>=1.17.0
tokenizers>=0.15.0
huggingface-hub>=0.22.0
numpy>=1.24.0

# ── KHÔNG có: torch, transformers, optimum ───────────────
# (3 package trên là nguyên nhân kéo 888MB torch)
